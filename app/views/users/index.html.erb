<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
  </head>
  <body>
    <ul>
     <%@users.each do |user| %>
        <li>
            <%= render partial: 'application/user_card', locals: { user: user } %>
        </li>
      <% end %>
    </ul>
  </body>
</html>